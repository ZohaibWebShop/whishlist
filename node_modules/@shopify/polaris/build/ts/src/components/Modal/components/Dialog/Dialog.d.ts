import React from 'react';
import type { SetStateAction, Dispatch } from 'react';
export interface DialogProps {
    labelledBy?: string;
    instant?: boolean;
    children?: React.ReactNode;
    limitHeight?: boolean;
    large?: boolean;
    small?: boolean;
    onClose(): void;
    onEntered?(): void;
    onExited?(): void;
    in?: boolean;
    fullScreen?: boolean;
    setClosing?: Dispatch<SetStateAction<boolean>>;
    hasToasts?: boolean;
}
export declare function Dialog({ instant, labelledBy, children, onClose, onExited, onEntered, large, small, limitHeight, fullScreen, setClosing, hasToasts, ...props }: DialogProps): React.JSX.Element;
//# sourceMappingURL=Dialog.d.ts.map